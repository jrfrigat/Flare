services:
  flare-gallery:
    build:
      context: .
      dockerfile: samples/Flare.Gallery/Dockerfile
    image: flare-gallery:latest
    restart: unless-stopped
    networks:
      - proxy_network


networks:
  proxy_network:
    external: true
