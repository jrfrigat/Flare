<!-- <auto-generated/> Generated by tools/Flare.ApiDocGen. Do not edit by hand. -->

# FlareSelect

`Flare.Components.FlareSelect`

Single-value dropdown over a list of items: a select-only combobox by default, or a filtering one with Searchable. Rows render as plain labels, as an ItemTemplate, or - with NullOption - as a pinned "no value" row, so a filter select expresses "all" without a sentinel value.

## Parameters

| Name | Type | Default | Kind | Required | Description |
| --- | --- | --- | --- | --- | --- |
| `ChildContent` | `RenderFragment?` | `` | Parameter |  | Declarative options as native <option> child markup (used instead of Items). |
| `Clearable` | `bool` | `false` | Parameter |  | Shows a clear button that resets the value. |
| `For` | `Expression<Func<TValue?>>?` | `` | Parameter |  | Expression used to bind and validate the field against an EditContext. |
| `Fuzzy` | `bool` | `false` | Parameter |  | Ranks filtered options by fuzzy relevance (via FlareSearch) instead of insertion order. |
| `GroupBy` | `Func<TValue?, string>?` | `` | Parameter |  | Returns the group label for an item; when non-null, options are grouped under headers. |
| `ItemDisabled` | `Func<TValue?, bool>?` | `` | Parameter |  | Returns whether an item is disabled (shown, never committable). |
| `ItemLabel` | `Func<TValue?, string>?` | `` | Parameter |  | Returns the display text for an item. When null, the declarative label or ToString() is used. |
| `ItemTemplate` | `RenderFragment<TValue?>?` | `` | Parameter |  | Custom render template for an item (dropdown row and, unless SelectedTemplate is set, the trigger). |
| `Items` | `IEnumerable<TValue?>?` | `` | Parameter |  | Collection of items to populate the dropdown. |
| `NoResultsText` | `string?` | `` | Parameter |  | Text shown in the dropdown when a search query matches no options. |
| `NullOption` | `string?` | `` | Parameter |  | Text of a selectable "no value" row prepended to the list - the "All ingredients" / "Any" case. Choosing it sets Value to null, and the closed field then shows this text rather than the placeholder, so a filter select needs no sentinel value of its own. The row is pinned: it stays at the top and survives typing, because a row meaning "no filter" is useless if searching hides it. Clearable composes with it - clearing selects it. Requires a nullable TValue. For a struct such as an int or an enum the default is a real value and cannot mean "none", so binding the nullable form is the only way to express it; setting this on a non-nullable value type throws rather than selecting zero. |
| `NullOptionTemplate` | `RenderFragment?` | `` | Parameter |  | Rich content for the NullOption row, used both in the list and in the closed field. Falls back to the NullOption text. |
| `Open` | `bool` | `false` | Parameter |  | Whether the dropdown is open (supports @bind-Open). |
| `OpenChanged` | `EventCallback<bool>` | `` | Callback |  | Callback invoked when the open state changes, enabling @bind-Open. |
| `RankFunc` | `Func<TValue?, string, double>?` | `` | Parameter |  | Custom relevance scorer (item, query) => score; overrides Fuzzy. |
| `Searchable` | `bool` | `false` | Parameter |  | Shows an in-field search input that filters the options as the user types. |
| `SelectedTemplate` | `RenderFragment<TValue?>?` | `` | Parameter |  | Custom render template for the selected value in the trigger. Falls back to ItemTemplate. |
| `Value` | `TValue?` | `` | Parameter |  | Currently selected value (supports @bind-Value). |
| `ValueChanged` | `EventCallback<TValue?>` | `` | Callback |  | Callback invoked when the selection changes, enabling @bind-Value. |
| `VirtualizeThreshold` | `int` | `50` | Parameter |  | Option count above which the dropdown virtualizes (when not grouped). Default: 50. |
| `AdditionalAttributes` | `IReadOnlyDictionary<string, object>?` | `` | Parameter |  | Additional attributes. Inherited from `FlareComponentBase`. |
| `Class` | `string?` | `` | Parameter |  | Additional CSS class(es) appended to the component's root element. Inherited from `FlareComponentBase`. |
| `Disabled` | `bool` | `false` | Parameter |  | Disables the field (no input, dimmed). Inherited from `FlareFieldBase`. |
| `ErrorText` | `string?` | `` | Parameter |  | Error text; when set it overrides HelperText and marks the field invalid. Inherited from `FlareFieldBase`. |
| `HelperText` | `string?` | `` | Parameter |  | Helper text shown below the field. Inherited from `FlareFieldBase`. |
| `InputAttributes` | `IReadOnlyDictionary<string, object>?` | `` | Parameter |  | Attributes splatted onto the field's inner control - the <input>, <textarea> or combobox element the user interacts with - rather than onto the wrapper. This is where data-testid, name, form, autofocus, tabindex and extra aria-* belong: an unmatched attribute written directly on the component lands on the field's root element, following the same rule as every other Flare component, and a test that targets it would be pointing at the wrapper. A field built from several equal inputs - the OTP field - has no single control and ignores this. Inherited from `FlareFieldBase`. |
| `Label` | `string?` | `` | Parameter |  | Label text shown for the field. Ignored when LabelContent is set. Inherited from `FlareFieldBase`. |
| `LabelContent` | `RenderFragment?` | `` | Parameter |  | Markup rendered as the field label instead of Label - a unit, a help affordance, a link in a consent line. Named rather than ChildContent because a field's content is its control, not its label. Inherited from `FlareFieldBase`. |
| `Placeholder` | `string?` | `` | Parameter |  | Placeholder text shown when the field is empty. Not every field renders it (e.g. toggles). Inherited from `FlareFieldBase`. |
| `ReadOnly` | `bool` | `false` | Parameter |  | Makes the field read-only (value shown but not editable). Not every field renders it. Inherited from `FlareFieldBase`. |
| `Required` | `bool` | `false` | Parameter |  | Marks the field as required (visual indicator + native required where applicable). Inherited from `FlareFieldBase`. |
| `Size` | `FieldSize` | `FieldSize.Md` | Parameter |  | Control size (Xs..Xl). Md (the default) is the standard field height. Inherited from `FlareFieldBase`. |
| `Style` | `string?` | `` | Parameter |  | Inline style string appended to the component's root element. Inherited from `FlareComponentBase`. |
| `Typo` | `TypographyScale?` | `` | Parameter |  | Optional typography scale for the field's text. Overrides the size-derived font; null (the default) keeps the size default. Inherited from `FlareFieldBase`. |
| `Variant` | `InputVariant` | `InputVariant.Default` | Parameter |  | Visual variant (Filled/Outlined) of the field, independent of the active theme. Default (the default) keeps the theme's own field style. Inherited from `FlareFieldBase`. |

## Methods

### `DisposeAsync()`

Returns: `ValueTask`.


## Inheritance

- `FlareFieldBase`
- `FlareComponentBase`
- `ComponentBase`
- `object`
