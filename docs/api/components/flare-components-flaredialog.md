<!-- <auto-generated/> Generated by tools/Flare.ApiDocGen. Do not edit by hand. -->

# FlareDialog

`Flare.Components.FlareDialog`

## Parameters

| Name | Type | Default | Kind | Required | Description |
| --- | --- | --- | --- | --- | --- |
| `ActionsContent` | `RenderFragment?` | `` | Parameter |  | Action buttons rendered in the dialog footer. |
| `AriaLabel` | `string?` | `` | Parameter |  | Accessible name for a header-less dialog (rendered as aria-label when Title is empty). Ignored when a title is present, since the title labels the dialog. |
| `BeforeClose` | `Func<DialogCloseReason, ValueTask<bool>>?` | `` | Parameter |  | Optional guard consulted before a scrim / Escape / close-button dismissal. Return true to allow the close or false to veto it (e.g. to keep the dialog open while there are unsaved changes). |
| `ChildContent` | `RenderFragment?` | `` | Parameter |  | Main body content rendered inside the dialog. |
| `CloseOnEsc` | `bool` | `true` | Parameter |  | Closes the dialog when the Escape key is pressed. |
| `CloseOnNavigation` | `bool` | `true` | Parameter |  | Dismisses the dialog automatically when the app navigates to a new route. Default true. |
| `CloseOnScrimClick` | `bool` | `true` | Parameter |  | Closes the dialog when the scrim backdrop is clicked. |
| `Divider` | `bool` | `false` | Parameter |  | Shows a divider between the header and the content. |
| `Draggable` | `bool` | `false` | Parameter |  | Lets the user move the dialog by dragging its header. |
| `IconContent` | `RenderFragment?` | `` | Parameter |  | Optional hero icon shown centered above the title. |
| `PanelClass` | `string?` | `` | Parameter |  | Extra CSS class(es) applied to the dialog panel. |
| `Position` | `DialogPosition` | `DialogPosition.Center` | Parameter |  | Where the dialog is anchored. Bottom renders a slide-up bottom sheet. |
| `Resizable` | `bool` | `false` | Parameter |  | Lets the user resize the dialog from a bottom-right gripper. |
| `ScrimClass` | `string?` | `` | Parameter |  | Extra CSS class(es) applied to the scrim/backdrop. |
| `ShowCloseButton` | `bool` | `false` | Parameter |  | Shows a built-in close (X) button in the top corner of the panel. |
| `ShowGrabber` | `bool` | `false` | Parameter |  | Shows a drag-grabber handle at the top (only for Bottom sheets). |
| `Size` | `DialogSize` | `DialogSize.Md` | Parameter |  | Maximum width of the dialog. |
| `Title` | `string?` | `` | Parameter |  | Title text displayed in the dialog header. |
| `Visible` | `bool` | `false` | Parameter |  | Controls whether the dialog is shown. |
| `VisibleChanged` | `EventCallback<bool>` | `` | Callback |  | Callback raised when visibility should change. |
| `AdditionalAttributes` | `IReadOnlyDictionary<string, object>?` | `` | Parameter |  | Additional attributes. Inherited from `FlareComponentBase`. |
| `Class` | `string?` | `` | Parameter |  | Additional CSS class(es) appended to the component's root element. Inherited from `FlareComponentBase`. |
| `FillHeight` | `bool` | `false` | Parameter |  | Makes the component spend the height it was given rather than growing to fit its content, and pass that height on to its children. This is the screen-fit case - a grid under a filter bar, a chart beside a table, a page that fills the window instead of scrolling with it. It needs an ancestor with a height of its own, and EVERY link between that ancestor and this component needs the same switch: one link left at auto collapses the chain back to content height, silently. A page gets its first definite height from FlareLayoutContent FillHeight; anywhere else - a demo, a dialog - give the outermost box one explicitly. The box that SUPPLIES a height does not fill: when Style declares a height of its own, that height wins and this switch is ignored, because the two are contradictory instructions and the written number is the one you can see in the markup. Inherited from `FlareContainerBase`. |
| `Style` | `string?` | `` | Parameter |  | Inline style string appended to the component's root element. Inherited from `FlareComponentBase`. |

## Methods

### `CloseFromEsc()`

Returns: `Task`.

### `DisposeAsync()`

Disposes the component; override to release JS interop or subscriptions.
Returns: `ValueTask`.


## Inheritance

- `FlareContainerBase`
- `FlareComponentBase`
- `ComponentBase`
- `object`
