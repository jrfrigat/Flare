<!-- <auto-generated/> Generated by tools/Flare.ApiDocGen. Do not edit by hand. -->

# FlareLazy

`Flare.Components.FlareLazy`

## Parameters

| Name | Type | Default | Kind | Required | Description |
| --- | --- | --- | --- | --- | --- |
| `ChildContent` | `RenderFragment?` | `` | Parameter |  | Content that is only rendered once this element scrolls near the viewport. |
| `KeepRendered` | `bool` | `true` | Parameter |  | When true (default) the content stays rendered once revealed -- the cheap, flicker-free mode that simply defers the initial mount. When false the content is unmounted again whenever it scrolls out of view, capping memory for very long pages at the cost of re-mounting (and losing transient UI state) on each pass. |
| `MinHeight` | `string?` | `` | Parameter |  | Minimum height (any CSS length) the wrapper reserves while the content is hidden, so the layout does not collapse before reveal. Dropped once ChildContent is shown (unless KeepRendered is false). Ignored when a PlaceholderContent sizes itself. |
| `OnRevealed` | `EventCallback` | `` | Callback |  | Raised the first time (and, when KeepRendered is false, each time) the content becomes visible. |
| `PlaceholderContent` | `RenderFragment?` | `` | Parameter |  | Optional content shown in place of ChildContent until it is revealed (e.g. a FlareSkeleton). When unset the wrapper is empty until reveal; combine with MinHeight so the page reserves space and the observer fires progressively. |
| `Root` | `string?` | `` | Parameter |  | CSS selector of the scrollable ancestor to watch instead of the browser viewport. Resolved with element.closest(selector), so it works when the content lives inside an overflow:auto panel (e.g. a side editor or dialog body) rather than the page itself. Null (default) observes the viewport. |
| `RootMargin` | `string` | `"200px"` | Parameter |  | IntersectionObserver rootMargin (CSS margin shorthand). Positive values reveal the content before it is actually on screen for a seamless scroll. Default "200px". |
| `AdditionalAttributes` | `IReadOnlyDictionary<string, object>?` | `` | Parameter |  | Additional attributes. Inherited from `FlareComponentBase`. |
| `Class` | `string?` | `` | Parameter |  | Additional CSS class(es) appended to the component's root element. Inherited from `FlareComponentBase`. |
| `Style` | `string?` | `` | Parameter |  | Inline style string appended to the component's root element. Inherited from `FlareComponentBase`. |

## Methods

### `DisposeAsync()`

Disposes the component; override to release JS interop or subscriptions.
Returns: `ValueTask`.

### `OnVisibilityChanged(bool visible)`

Invoked by the IntersectionObserver when the wrapper enters or leaves the viewport.
Returns: `Task`.

| Parameter | Type | Description |
| --- | --- | --- |
| `visible` | `bool` | — |


## Inheritance

- `FlareComponentBase`
- `ComponentBase`
- `object`
