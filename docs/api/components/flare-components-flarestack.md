<!-- <auto-generated/> Generated by tools/Flare.ApiDocGen. Do not edit by hand. -->

# FlareStack

`Flare.Components.FlareStack`

## Parameters

| Name | Type | Default | Kind | Required | Description |
| --- | --- | --- | --- | --- | --- |
| `Align` | `FlareAlignItems` | `FlareAlignItems.Default` | Parameter |  | Cross-axis alignment of children. |
| `ChildContent` | `RenderFragment?` | `` | Parameter |  | Child content rendered inside the stack. |
| `Gap` | `FlareSpacing` | `FlareSpacing.Medium` | Parameter |  | Spacing between child elements using design-token values. |
| `GapValue` | `string?` | `` | Parameter |  | Raw CSS gap value used when Gap is Custom. |
| `Justify` | `FlareJustifyContent` | `FlareJustifyContent.Default` | Parameter |  | Main-axis justification of children. |
| `Reverse` | `bool` | `false` | Parameter |  | Flex direction - reverse or not (default). |
| `Row` | `bool` | `false` | Parameter |  | Flex direction - row or column (default). |
| `StretchFirst` | `bool` | `false` | Parameter |  | When true, only the first direct child grows to fill the remaining main-axis space (flex: 1 1 auto) while the others keep their natural size - e.g. a title that expands to push trailing actions to the edge. Ignored when StretchItems is set. |
| `StretchItems` | `bool` | `false` | Parameter |  | When true, every direct child grows to share the available space equally along the main axis (each child gets flex: 1 1 0). Use it to make items fill a row/column evenly. |
| `StretchLast` | `bool` | `false` | Parameter |  | When true, only the last direct child grows to fill the remaining main-axis space (flex: 1 1 auto) while the others keep their natural size - e.g. a fixed leading rail with a content pane that fills the rest. Ignored when StretchItems or StretchFirst is set. |
| `Wrap` | `bool` | `false` | Parameter |  | Allows children to wrap onto multiple lines. |
| `AdditionalAttributes` | `IReadOnlyDictionary<string, object>?` | `` | Parameter |  | Additional attributes. Inherited from `FlareComponentBase`. |
| `Class` | `string?` | `` | Parameter |  | Additional CSS class(es) appended to the component's root element. Inherited from `FlareComponentBase`. |
| `FillHeight` | `bool` | `false` | Parameter |  | Makes the component spend the height it was given rather than growing to fit its content, and pass that height on to its children. This is the screen-fit case - a grid under a filter bar, a chart beside a table, a page that fills the window instead of scrolling with it. It needs an ancestor with a height of its own, and EVERY link between that ancestor and this component needs the same switch: one link left at auto collapses the chain back to content height, silently. A page gets its first definite height from FlareLayoutContent FillHeight; anywhere else - a demo, a dialog - give the outermost box one explicitly. The box that SUPPLIES a height does not fill: when Style declares a height of its own, that height wins and this switch is ignored, because the two are contradictory instructions and the written number is the one you can see in the markup. Inherited from `FlareContainerBase`. |
| `Style` | `string?` | `` | Parameter |  | Inline style string appended to the component's root element. Inherited from `FlareComponentBase`. |

## Methods

This component exposes no documented public methods.

## Inheritance

- `FlareContainerBase`
- `FlareComponentBase`
- `ComponentBase`
- `object`
