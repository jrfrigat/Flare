<!-- <auto-generated/> Generated by tools/Flare.ApiDocGen. Do not edit by hand. -->

# FlareSlider

`Flare.Components.FlareSlider`

## Parameters

| Name | Type | Default | Kind | Required | Description |
| --- | --- | --- | --- | --- | --- |
| `AriaLabel` | `string?` | `` | Parameter |  | Accessible label used when no visible Label is provided. |
| `Color` | `FlareColor` | `` | Parameter |  | Accent color of the active track and handle. Role (FlareColor.Primary) -> shared class; custom (FlareColor.Custom("#...")) -> inline token. Default uses the theme primary. |
| `Disabled` | `bool` | `false` | Parameter |  | Disables the slider when true. |
| `EndIcon` | `FlareIcon?` | `` | Parameter |  | Icon shown after the track - any provider (a bare string is a Material Symbols name). |
| `ErrorText` | `string?` | `` | Parameter |  | Overrides validation with a custom error message. |
| `For` | `Expression<Func<double>>?` | `` | Parameter |  | Expression used to bind and validate the field. |
| `Format` | `string?` | `` | Parameter |  | Format string applied to the displayed value(s). |
| `HandleOnHover` | `bool` | `false` | Parameter |  | Hides the handle until the slider is hovered or focused, the way a media player's seek bar behaves. Only the handle's paint is hidden: the control keeps its full hit area, stays in the tab order and still reads its value, so nothing about reaching it changes. |
|  |  |  |  |  | Ignored where the pointer cannot hover - a touch user has no way to reveal the handle, so on a coarse pointer it stays visible. Combine with Zones for a buffered band and MouseWheel for wheel-to-seek to get a player scrubber out of the plain slider. |
| `HelperText` | `string?` | `` | Parameter |  | Helper text shown below the slider. |
| `Indicator` | `bool` | `false` | Parameter |  | Shows the floating value indicator above the handle(s) while interacting. |
| `Init` | `double?` | `` | Parameter |  | Anchor the fill starts from (single mode). The active track is drawn between Init and Value; defaults to Min. Set to a mid value (e.g. 0 for a Min=-100/Max=100 left/right balance slider) so the fill grows out from that point. Ignored in range mode. |
| `Label` | `string?` | `` | Parameter |  | Label text displayed above the slider. Ignored when LabelContent is set. |
| `LabelContent` | `RenderFragment?` | `` | Parameter |  | Markup rendered as the label instead of Label - a unit, a link, an inline badge. Named rather than ChildContent because the label is one part of the slider among several, so content written between the tags would not say which part it meant. |
| `Marks` | `IReadOnlyDictionary<double, string>?` | `` | Parameter |  | Labeled marks placed under the track at specific values (value -> label), e.g. { [0]="Low", [50]="Mid", [100]="High" }. A mark is highlighted when it falls within the selected span. Values outside [Min, Max] are ignored. |
| `Max` | `double` | `100` | Parameter |  | Maximum value of the slider range. |
| `Min` | `double` | `0` | Parameter |  | Minimum value of the slider range. |
| `MouseWheel` | `bool` | `false` | Parameter |  | When true, hovering the slider and turning the mouse wheel adjusts Value: each notch moves by Step (scroll up increases, down decreases) and page scrolling is suppressed over the track. In range mode a plain wheel moves the low handle (Value) and Ctrl+wheel moves the high handle (ValueEnd); neither handle can cross the other. No effect when Disabled. |
| `Range` | `bool` | `false` | Parameter |  | Enables a two-handle range slider (Value = low, ValueEnd = high). |
| `ShowValue` | `bool` | `true` | Parameter |  | Shows the current value (or low-high range) next to the label when true. |
| `Size` | `TrackSize` | `TrackSize.Xs` | Parameter |  | Size variant (XSmall-XLarge). Controls track thickness and handle height. |
| `StartIcon` | `FlareIcon?` | `` | Parameter |  | Icon shown before the track - any provider (a bare string is a Material Symbols name). |
| `Step` | `double` | `1` | Parameter |  | Increment step between slider positions. Defaults to 1; any positive decimal step is allowed. |
| `Stepper` | `bool` | `false` | Parameter |  | Shows step (stop) indicators along the track at each Step position. |
| `Value` | `double` | `0` | Parameter |  | Current value of the slider. In range mode this is the lower bound. |
| `ValueChanged` | `EventCallback<double>` | `` | Callback |  | Callback invoked when Value changes. |
| `ValueEnd` | `double` | `100` | Parameter |  | Upper bound of the selected range (range mode only). |
| `ValueEndChanged` | `EventCallback<double>` | `` | Callback |  | Callback invoked when ValueEnd changes. |
| `Vertical` | `bool` | `false` | Parameter |  | Renders the slider vertically (value increases upward). Length is set via the --flare-slider-length CSS var (default 12rem). |
| `Zones` | `RenderFragment?` | `` | Parameter |  | Declarative colored zones on the track: one or more FlareZone children, each a static [Start,End] range on the Min..Max scale in its own color. Zones are read-only scale annotations (safe/warning/danger ranges, a media buffer band, per-step coloring) drawn on top of the inactive track but under the active fill, so the accent fill always shows the current value on top. |
| `AdditionalAttributes` | `IReadOnlyDictionary<string, object>?` | `` | Parameter |  | Additional attributes. Inherited from `FlareComponentBase`. |
| `Class` | `string?` | `` | Parameter |  | Additional CSS class(es) appended to the component's root element. Inherited from `FlareComponentBase`. |
| `Style` | `string?` | `` | Parameter |  | Inline style string appended to the component's root element. Inherited from `FlareComponentBase`. |

## Methods

### `DisposeAsync()`

Disposes the component; override to release JS interop or subscriptions.
Returns: `ValueTask`.


## Inheritance

- `FlareComponentBase`
- `ComponentBase`
- `object`
