<!-- <auto-generated/> Generated by tools/Flare.ApiDocGen. Do not edit by hand. -->

# FlareGauge

`Flare.Components.FlareGauge`

One value read against a marked scale: a needle dial, a filled KPI arc, or a straight bar with ticks. FlareProgress and FlareMeter are both bars and answer "how far along"; a gauge answers "where does this sit on the scale, and is that good", which is the dashboard reading.

## Remarks

Coloured bands are FlareZone children - the same Start/End-on-a-host-scale primitive FlareSlider and FlareProgress already take, rather than a gauge-specific range type. Which part of a scale counts as bad belongs to the application's data, so it arrives as a zone with a colour rather than as a token a theme would have to guess at. No JS: the whole gauge is one SVG whose geometry is computed in C# and whose every colour and thickness is a token read by the stylesheet.

## Parameters

| Name | Type | Default | Kind | Required | Description |
| --- | --- | --- | --- | --- | --- |
| `Animate` | `bool` | `true` | Parameter |  | Sweeps the fill and needle to the value on first render and on every change. Default true; a reduced-motion preference suppresses it regardless. |
| `AriaLabel` | `string?` | `` | Parameter |  | Describes the gauge for assistive technology. Falls back to Label; the value, range and band the value falls in are announced from the ARIA meter attributes either way. |
| `ChildContent` | `RenderFragment?` | `` | Parameter |  | Zones drawn as coloured bands behind the fill, declared as FlareZone children. |
| `Color` | `FlareColor` | `` | Parameter |  | Colour of the fill (and of the needle when one is drawn). Default takes the theme's. |
| `EndAngle` | `double?` | `` | Parameter |  | Where the scale ends, in degrees clockwise from twelve o'clock. Defaults to 90 for an arc and 135 for a radial dial. A 360-degree sweep gives the progress-ring case. |
| `Format` | `string?` | `` | Parameter |  | Format applied to the value and the scale labels (for example "N0", "P0"). |
| `Label` | `string?` | `` | Parameter |  | Caption under the readout - what the gauge is measuring. |
| `Max` | `double` | `100` | Parameter |  | High end of the scale. Default 100. |
| `Min` | `double` | `0` | Parameter |  | Low end of the scale. Default 0. |
| `MinorTickInterval` | `double` | `0` | Parameter |  | Distance between minor ticks. Zero (default) draws none. |
| `Needle` | `bool?` | `` | Parameter |  | Reads the value with a needle rather than by filling the track. Defaults to true for Radial and false for the other two - a needle is the dial's own idiom, and a filled arc is the KPI one. |
| `Shape` | `GaugeShape` | `GaugeShape.Arc` | Parameter |  | How the scale is laid out. Default Arc. |
| `ShowScaleLabels` | `bool` | `false` | Parameter |  | Labels the major ticks with their values. Default false - a dial with a big readout in the middle rarely needs the scale spelled out as well. |
| `ShowTicks` | `bool` | `true` | Parameter |  | Draws the tick marks. Default true. |
| `ShowValue` | `bool` | `true` | Parameter |  | Draws the value in the middle of the gauge. Default true. |
| `StartAngle` | `double?` | `` | Parameter |  | Where the scale starts, in degrees clockwise from twelve o'clock. Ignored by Linear. Defaults to -90 (nine o'clock) for an arc and -135 for a radial dial, which puts the dial's gap at the bottom. |
| `Target` | `double?` | `` | Parameter |  | A second value marked on the scale: the target, the budget, last month's figure. Drawn as a marker across the track rather than as a second needle, because a gauge answers one question. |
| `TickInterval` | `double` | `0` | Parameter |  | Distance between major ticks, on the scale's own units. Default: a fifth of the scale. |
| `Value` | `double` | `0` | Parameter |  | The value to read against the scale. Values outside the scale pin to its ends. |
| `ValueTemplate` | `RenderFragment<double>?` | `` | Parameter |  | Replaces the readout with custom content, receiving the current value. |
| `Vertical` | `bool` | `false` | Parameter |  | Lays a Linear gauge bottom-to-top instead of left-to-right. |
| `AdditionalAttributes` | `IReadOnlyDictionary<string, object>?` | `` | Parameter |  | Additional attributes. Inherited from `FlareComponentBase`. |
| `Class` | `string?` | `` | Parameter |  | Additional CSS class(es) appended to the component's root element. Inherited from `FlareComponentBase`. |
| `Style` | `string?` | `` | Parameter |  | Inline style string appended to the component's root element. Inherited from `FlareComponentBase`. |

## Methods

This component exposes no documented public methods.

## Inheritance

- `FlareComponentBase`
- `ComponentBase`
- `object`
