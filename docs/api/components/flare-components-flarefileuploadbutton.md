<!-- <auto-generated/> Generated by tools/Flare.ApiDocGen. Do not edit by hand. -->

# FlareFileUploadButton

`Flare.Components.FlareFileUploadButton`

## Parameters

| Name | Type | Default | Kind | Required | Description |
| --- | --- | --- | --- | --- | --- |
| `AriaLabel` | `string?` | `` | Parameter |  | Accessible label for the trigger. |
| `ChildContent` | `RenderFragment?` | `` | Parameter |  | Button content, replacing Text. |
| `Color` | `FlareColor` | `` | Parameter |  | Semantic color of the button. |
| `FullWidth` | `bool` | `false` | Parameter |  | Stretches the button to the full width of its container. |
| `LeadingIcon` | `RenderFragment?` | `` | Parameter |  | Icon before the label. Defaults to an upload glyph; set it to say what the button imports. |
| `Loading` | `bool` | `false` | Parameter |  | Shows a busy state and blocks the picker - for a caller that is still reading or posting the previous selection. |
| `LoadingTemplate` | `RenderFragment?` | `` | Parameter |  | Custom content shown while Loading is true, replacing the default spinner (and LoadingText). Use it for a bespoke loading indicator. |
| `LoadingText` | `RenderFragment?` | `` | Parameter |  | Label shown while Loading. Null keeps the normal label. |
| `OnColor` | `string?` | `` | Parameter |  | Optional foreground color for the filled variant when Color is a custom color. |
| `Shape` | `ButtonShape` | `ButtonShape.Default` | Parameter |  | Corner shape of the button. |
| `Size` | `ButtonSize` | `ButtonSize.Md` | Parameter |  | Size scale, exactly as on FlareButton - so an upload button lines up with the buttons beside it in a toolbar. Default medium. |
| `Text` | `string?` | `` | Parameter |  | Button label. Defaults to the localized "choose file" text; ChildContent overrides it. |
| `TrailingIcon` | `RenderFragment?` | `` | Parameter |  | Icon after the label. |
| `Typo` | `TypographyScale?` | `` | Parameter |  | Optional label type-scale override, as on FlareButton. Null keeps the size-based typography. |
| `Variant` | `ButtonVariant` | `ButtonVariant.Outlined` | Parameter |  | Visual variant, exactly as on FlareButton. Default outlined. |
| `Accept` | `string?` | `` | Parameter |  | Accepted file types as a MIME or extension filter string. Inherited from `FlareFileUploadBase`. |
| `AdditionalAttributes` | `IReadOnlyDictionary<string, object>?` | `` | Parameter |  | Additional attributes. Inherited from `FlareComponentBase`. |
| `AllowCancel` | `bool` | `true` | Parameter |  | Shows a cancel affordance on a file that is still queued or uploading. Default true. Inherited from `FlareFileUploadBase`. |
| `AllowRemove` | `bool` | `true` | Parameter |  | Shows a remove affordance that drops a file from the list entirely. Default true. Removing a file that is still transferring cancels it first - there is no way to un-send bytes, so the row leaves and the request is aborted. Inherited from `FlareFileUploadBase`. |
| `AllowRetry` | `bool` | `true` | Parameter |  | Shows a retry affordance on a file that failed or was cancelled. Default true. Inherited from `FlareFileUploadBase`. |
| `Auto` | `bool` | `false` | Parameter |  | Starts the queue as soon as files are selected instead of waiting for UploadAsync. Ignored when Uploader is null. Inherited from `FlareFileUploadBase`. |
| `Class` | `string?` | `` | Parameter |  | Additional CSS class(es) appended to the component's root element. Inherited from `FlareComponentBase`. |
| `Concurrency` | `int` | `1` | Parameter |  | How many files may transfer at once. Default 1 - a server is more likely to be annoyed by a burst than by a queue. Inherited from `FlareFileUploadBase`. |
| `Disabled` | `bool` | `false` | Parameter |  | Disables the control - the trigger stops opening the file picker. Inherited from `FlareFileUploadBase`. |
| `FileTemplate` | `RenderFragment<FlareUploadFile>?` | `` | Parameter |  | Replaces the whole row. Receives the queue entry, so an application can render its own layout, thumbnail or affordances against the same state the built-in row reads. Inherited from `FlareFileUploadBase`. |
| `MaxFileSize` | `long` | `9223372036854775807` | Parameter |  | Largest accepted file, in bytes. Anything over it is dropped from the selection before OnFilesChanged sees it. Unlimited by default: a cap discards the user's file with no explanation, so it is the caller - who knows what the server will take - that opts in. Filtering here is UX only; always check the size again on the server. Inherited from `FlareFileUploadBase`. |
| `MaxFiles` | `int` | `10` | Parameter |  | Maximum number of files that can be selected. Inherited from `FlareFileUploadBase`. |
| `Multiple` | `bool` | `false` | Parameter |  | Allows selecting multiple files when true. Inherited from `FlareFileUploadBase`. |
| `OnAllCompleted` | `EventCallback` | `` | Callback |  | Raised once when no file is queued or uploading any more, whatever the outcomes. Inherited from `FlareFileUploadBase`. |
| `OnFilesChanged` | `EventCallback<IReadOnlyList<IBrowserFile>>` | `` | Callback |  | Callback invoked with the list of selected files. Inherited from `FlareFileUploadBase`. |
| `OnUploadCompleted` | `EventCallback<FlareUploadFile>` | `` | Callback |  | Raised when one file finishes successfully. Inherited from `FlareFileUploadBase`. |
| `OnUploadFailed` | `EventCallback<FlareUploadFile>` | `` | Callback |  | Raised when one file's transfer throws. The exception is not rethrown - the row carries the message and the queue continues. Inherited from `FlareFileUploadBase`. |
| `OnUploadProgress` | `EventCallback<FlareUploadFile>` | `` | Callback |  | Raised as bytes move, at whatever rate the transfer reports them. Marshalled onto the renderer's dispatcher, so a handler may touch component state directly. Inherited from `FlareFileUploadBase`. |
| `OnUploadStarted` | `EventCallback<FlareUploadFile>` | `` | Callback |  | Raised when one file's transfer begins - after it leaves the queue, before any bytes move. Inherited from `FlareFileUploadBase`. |
| `ShowFileList` | `bool` | `true` | Parameter |  | Shows the list of selected files under the trigger. Default true. Inherited from `FlareFileUploadBase`. |
| `Style` | `string?` | `` | Parameter |  | Inline style string appended to the component's root element. Inherited from `FlareComponentBase`. |
| `Uploader` | `Func<FlareUploadContext, Task>?` | `` | Parameter |  | The transfer itself. Given a FlareUploadContext - the file, a progress sink and a cancellation token - send the bytes however the application sends bytes, and report the running total to Progress. Inherited from `FlareFileUploadBase`. |
|  |  |  |  |  | A delegate rather than a URL on purpose. A URL plus headers cannot express a token that refreshes mid-upload, a presigned PUT, a resumable protocol or an application's own retry policy - and an upload component that gets those wrong is one an application has to wrap. Flare owns the queue, the concurrency, the cancellation and the whole visual state; the application owns the wire. Leave it null and the component is exactly what it was before: a file picker. |

## Methods

This component exposes no documented public methods.

## Inheritance

- `FlareFileUploadBase`
- `FlareComponentBase`
- `ComponentBase`
- `object`
