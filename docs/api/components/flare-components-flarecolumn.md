<!-- <auto-generated/> Generated by tools/Flare.ApiDocGen. Do not edit by hand. -->

# FlareColumn

`Flare.Components.FlareColumn`

One data column of a FlareDataGrid: what to read from a row, how to display and edit it, and which of sorting, filtering, grouping and aggregation it takes part in. Declaring it registers the column with the enclosing grid, band or composite row - the markup is a description, not the cells themselves, which the grid draws.

## Parameters

| Name | Type | Default | Kind | Required | Description |
| --- | --- | --- | --- | --- | --- |
| `Align` | `ColumnAlign?` | `` | Parameter |  | Horizontal alignment of this column's header and cells. When null (the default) the alignment is derived from Type (or the inferred data type): numbers align to the trailing edge, booleans center, everything else leads. |
| `ClassFunc` | `Func<TItem?, string>?` | `` | Parameter |  | Returns extra CSS class(es) for this column's cell, given the row item. Use for value-driven cell formatting (e.g. red text on negative numbers). |
| `ColSpan` | `int` | `1` | Parameter |  | Grid-column span for this field inside a composite row (default 1). |
| `Composite` | `RenderFragment?` | `` | Parameter |  | Composite layout: nested FlareColumnRow rows of field columns. When set, this column is a layout host (no Field). See CompositeMode. |
| `CompositeMode` | `CompositeMode` | `CompositeMode.Banded` | Parameter |  | How the composite lays out: Banded (default; each record spans several table rows, fields align across the table) or Card (all fields in one cell as a CSS grid). |
| `EditTemplate` | `RenderFragment<TItem?>?` | `` | Parameter |  | Custom edit template rendered when a cell is in edit mode. |
| `Editable` | `bool` | `false` | Parameter |  | Marks the column cells as editable. |
| `Field` | `Func<TItem?, object?>?` | `` | Parameter |  | Value selector used for sorting and default cell text. |
| `FilterDebounceMs` | `int?` | `` | Parameter |  | Debounce delay in milliseconds for this column's filter input. Set to 0 for instant filtering. Overrides the grid-level FilterDebounceMs when set. |
| `FilterFunc` | `Func<TItem?, string, bool>?` | `` | Parameter |  | Custom filter predicate for this column, overriding the built-in text/number/date match. Receives the row and the current filter text; return true to keep the row. Use for cross-field or computed filtering that the default per-cell match cannot express. |
| `FilterOptions` | `IEnumerable<string>?` | `` | Parameter |  | Explicit option values for a Select filter. When null, the distinct cell values of the column are used. |
| `FilterType` | `ColumnFilterType?` | `` | Parameter |  | Type of inline filter editor shown when Filterable is true (text box, dropdown, number or date). When null (the default) the editor is derived from Type (or the inferred data type): boolean/enum get a dropdown, numbers a number box, dates a date box. |
| `Filterable` | `bool` | `false` | Parameter |  | Shows a filter input in the column header filter row. |
| `Format` | `string?` | `` | Parameter |  | Optional .NET format string applied to the cell value, e.g. "N2", "C" (currency) or "yyyy-MM-dd". Applied with the current culture. |
| `Id` | `string?` | `` | Parameter |  | Stable column identity used to key all grid state (sort, filters, visibility, order, persistence) and the server sort/filter key. Defaults to SortKey then Title. Set this only to disambiguate columns that share a title (a localized or duplicated header) on a purely client-side grid. |
| `NullText` | `string?` | `` | Parameter |  | Text rendered when the cell value is null (default: empty string). |
| `Resizable` | `bool` | `false` | Parameter |  | Allows the user to resize this column by dragging. |
| `SortComparison` | `Comparison<TItem?>?` | `` | Parameter |  | Custom row comparison used when this column is sorted, overriding the default comparison of Field values. Receives two rows; return a value like Comparison (negative/zero/positive). Ascending uses the result as-is, descending negates it. Use for domain-specific ordering (e.g. a custom status order) that a property comparison cannot express. |
| `SortKey` | `string?` | `` | Parameter |  | Sort key sent to the server-side items provider. |
| `SortPriority` | `int` | `0` | Parameter |  | Initial sort priority in multi-column sort order. |
| `Sortable` | `bool` | `false` | Parameter |  | Enables click-to-sort on this column. |
| `StyleFunc` | `Func<TItem?, string>?` | `` | Parameter |  | Returns an inline style string for this column's cell, given the row item. |
| `Template` | `RenderFragment<TItem?>?` | `` | Parameter |  | Custom cell render template overriding Field output. |
| `Type` | `ColumnDataType` | `ColumnDataType.Auto` | Parameter |  | Semantic data type of the column. Drives type-aware cell rendering (boolean as a checkbox icon, culture- and Format-aware dates and numbers) and the default inline filter editor. The default Auto infers the type from the runtime cell value. Ignored when Template is set. |
| `AdditionalAttributes` | `IReadOnlyDictionary<string, object>?` | `` | Parameter |  | Captures any unmatched HTML attributes. Inherited from `FlareColumnBase`. |
| `CssClass` | `string?` | `` | Parameter |  | Custom CSS class for the column/band header. Inherited from `FlareColumnBase`. |
| `Frozen` | `bool` | `false` | Parameter |  | Sticks this column to the left edge when the table scrolls horizontally. Inherited from `FlareColumnBase`. |
| `FrozenRight` | `bool` | `false` | Parameter |  | Sticks this column to the right edge when the table scrolls horizontally. Inherited from `FlareColumnBase`. |
| `Title` | `string` | `""` | Parameter |  | Header text displayed in the column/band heading, and the name the column is known by everywhere else - the export, the filter menu, the column picker, aggregates. Still required when TitleContent is set, because those places need text. Inherited from `FlareColumnBase`. |
| `TitleContent` | `RenderFragment?` | `` | Parameter |  | Markup painted in the heading instead of Title - a unit, an icon, a help affordance. Display only; Title keeps carrying the column's name. Inherited from `FlareColumnBase`. |
| `Width` | `string?` | `` | Parameter |  | Fixed CSS width applied to the column/band. Inherited from `FlareColumnBase`. |

## Methods

### `Dispose()`

Returns: `void`.


## Inheritance

- `FlareColumnBase`
- `ComponentBase`
- `object`
