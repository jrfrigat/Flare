<!-- <auto-generated/> Generated by tools/Flare.ApiDocGen. Do not edit by hand. -->

# FlareCheckbox

`Flare.Components.FlareCheckbox`

## Parameters

| Name | Type | Default | Kind | Required | Description |
| --- | --- | --- | --- | --- | --- |
| `CheckedIcon` | `RenderFragment?` | `` | Parameter |  | Custom icon shown when checked (replaces the default check mark). |
| `ChildContent` | `RenderFragment?` | `` | Parameter |  | Markup rendered as the label instead of Label, for a label that is more than a string - a link in a consent line, a unit, an inline badge. Writing it between the tags binds here. |
| `Color` | `FlareColor` | `` | Parameter |  | Accent color of the checked indicator (role -> shared class, custom -> inline token). Default = primary. |
| `Disabled` | `bool` | `false` | Parameter |  | Disables the checkbox when true. |
| `ErrorText` | `string?` | `` | Parameter |  | Overrides validation with a custom error message. |
| `For` | `Expression<Func<TValue?>>?` | `` | Parameter |  | Expression naming the model property this box edits, so the enclosing form can validate it and show the message. It is typed TValue like Value, so the two always name the same property - point both at a bool for a consent box (@bind-Value="Model.Accepted" For="@(() => Model.Accepted)"). |
| `IndeterminateIcon` | `RenderFragment?` | `` | Parameter |  | Custom icon shown when indeterminate. |
| `Label` | `string?` | `` | Parameter |  | Label text displayed next to the checkbox. Ignored when ChildContent is set. |
| `LabelPlacement` | `LabelPlacement` | `LabelPlacement.End` | Parameter |  | Label position relative to the checkbox (End = after, Start = before). Default End. |
| `Size` | `FieldSize` | `FieldSize.Md` | Parameter |  | Control size (Xs..Xl) on the shared scale; medium is the default. Each theme decides what a step measures. |
| `UncheckedIcon` | `RenderFragment?` | `` | Parameter |  | Custom icon shown when unchecked. |
| `Value` | `TValue?` | `` | Parameter |  | Whether the box is checked. With TValue="bool?" the value is tri-state and null is the indeterminate ("some of the below") state; with TValue="bool" there are two states and the box can be validated against a plain bool on the model. |
| `ValueChanged` | `EventCallback<TValue?>` | `` | Callback |  | Callback invoked when the checked state changes. A click always produces true or false - it resolves the indeterminate state rather than cycling back into it. |
| `AdditionalAttributes` | `IReadOnlyDictionary<string, object>?` | `` | Parameter |  | Additional attributes. Inherited from `FlareComponentBase`. |
| `Class` | `string?` | `` | Parameter |  | Additional CSS class(es) appended to the component's root element. Inherited from `FlareComponentBase`. |
| `Style` | `string?` | `` | Parameter |  | Inline style string appended to the component's root element. Inherited from `FlareComponentBase`. |

## Methods

### `DisposeAsync()`

Disposes the component; override to release JS interop or subscriptions.
Returns: `ValueTask`.


## Inheritance

- `FlareComponentBase`
- `ComponentBase`
- `object`
