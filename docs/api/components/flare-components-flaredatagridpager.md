<!-- <auto-generated/> Generated by tools/Flare.ApiDocGen. Do not edit by hand. -->

# FlareDataGridPager

`Flare.Components.FlareDataGridPager`

Page navigation for a FlareDataGrid, placed wherever the layout wants it rather than where the grid would put it. The grid keeps owning the page and page size - the data concern - while this owns how paging is presented. It renders nothing when there is nothing to navigate: a virtualized or infinite-scroll grid, or a single page with no page-size choice.

## Parameters

| Name | Type | Default | Kind | Required | Description |
| --- | --- | --- | --- | --- | --- |
| `BoundaryCount` | `int` | `1` | Parameter |  | How many page buttons to always show at each end (passed to the pagination control). |
| `RowsLabel` | `string?` | `` | Parameter |  | Accessible label / rows-per-page caption. When null (the default), the grid's RowsLabel is used, then a localized fallback. |
| `RowsPerPageOptions` | `IReadOnlyList<int>?` | `` | Parameter |  | Page-size choices offered in the rows-per-page dropdown. When null (the default), the grid's RowsPerPageOptions are used; set this to control the options from the pager instead. A single option (or none) hides the dropdown. |
| `ShowFirstLast` | `bool` | `false` | Parameter |  | Shows first/last jump buttons in addition to prev/next. |
| `SiblingCount` | `int` | `2` | Parameter |  | How many page buttons to show on each side of the current page (passed to the pagination control). |
| `Size` | `PaginationSize` | `PaginationSize.Md` | Parameter |  | Visual size of the pagination control. |
| `Context` | `DataGridContext<TItem?>?` | `` | Parameter |  | The grid context this control drives. Set it to place the control anywhere on the page; omit it inside a grid, where the cascade supplies one. Inherited from `FlareDataGridControl`. |
| `Grid` | `FlareDataGrid<TItem?>?` | `` | Parameter |  | The grid this control drives, as an alternative to Context when the page already holds an @ref to the grid itself. Inherited from `FlareDataGridControl`. |

## Methods

This component exposes no documented public methods.

## Inheritance

- `FlareDataGridControl`
- `ComponentBase`
- `object`
