namespace Flare.Components;

/// <summary>Visual style of the <c>FlareTimePicker</c> popup.</summary>
public enum TimePickerVariant
{
    /// <summary>Analog clock dial (12-hour with AM/PM). Click numbers to select hour then minute.</summary>
    Dial,
    /// <summary>Compact 24-hour hour/minute columns (scrollbar hidden).</summary>
    Dropdown,
}
