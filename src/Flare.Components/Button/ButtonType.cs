namespace Flare.Components;

/// <summary>HTML button type attribute values.</summary>
public enum ButtonType
{
    /// <summary>Standard clickable button (default).</summary>
    Button,
    /// <summary>Submits the parent form.</summary>
    Submit,
    /// <summary>Resets all form fields to their default values.</summary>
    Reset,
}
