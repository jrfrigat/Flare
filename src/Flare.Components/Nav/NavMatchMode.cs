namespace Flare.Components;

/// <summary>How a nav link's href is matched against the current URL to decide the active state.</summary>
public enum NavMatchMode
{
    /// <summary>Prefix.</summary>
    Prefix,
    /// <summary>Exact.</summary>
    Exact,
}
