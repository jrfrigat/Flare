namespace Flare.Components;

/// <summary>
/// One option of a <c>FlareChoiceGroup</c>: a card with a title, a hint and a radio control, laid out
/// by the group. It takes its name, its selected and its disabled state from the enclosing group, so
/// the group stays the one place the answer lives.
/// </summary>
/// <typeparam name="TValue">Type of the value this card contributes to the group.</typeparam>
public partial class FlareChoiceCard<TValue>;
