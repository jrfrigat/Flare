namespace Flare.Components;

/// <summary>Side of the content the <see cref="FlareTabs"/> bar is placed on.</summary>
public enum TabsPlacement
{
    /// <summary>Tab bar above the panels (horizontal). Default.</summary>
    Top,
    /// <summary>Tab bar below the panels (horizontal).</summary>
    Bottom,
    /// <summary>Tab bar to the left of the panels (vertical).</summary>
    Left,
    /// <summary>Tab bar to the right of the panels (vertical).</summary>
    Right,
}
