namespace Flare.Components;

/// <summary>
/// The month calendar shared by the date, date-time and range pickers: the weekday header and the
/// six-by-seven grid of day buttons. Each picker keeps its own selection and keyboard behaviour and
/// supplies it through the decoration hooks, so the markup and the date arithmetic live here once.
/// </summary>
public partial class FlareMonthGrid;
