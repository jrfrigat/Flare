namespace Flare.Components;

/// <summary>
/// Keyboard-shortcut scope: the entries declared inside it register their key combinations here, and it
/// listens for them, dispatches to the matching entry and can show the resulting list as a help overlay.
/// </summary>
public partial class FlareShortcuts;
