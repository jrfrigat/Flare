namespace Flare.Components;

/// <summary>
/// One stage of a <c>FlareStepper</c>: its label, its optional description, and the content shown while
/// it is the active stage. It registers with the stepper and draws nothing on its own - the stepper
/// renders both the indicator and the panel.
/// </summary>
public partial class FlareStep;
