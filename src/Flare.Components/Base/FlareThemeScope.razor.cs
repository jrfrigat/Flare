namespace Flare.Components;

/// <summary>
/// Re-themes one subtree without disturbing the rest of the app - a dark toolbar over a light page, or
/// a preview pane showing another design system. Each axis left unset keeps the surrounding theme's
/// answer, so a scope can change only the mode, only the palette, or all three.
/// </summary>
public partial class FlareThemeScope;
